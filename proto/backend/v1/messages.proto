syntax = "proto3";
package backend;

option go_package = ".;pb";

message Connection {
    string user_id = 1;
    string websocket = 2;
    string resource = 3;

    // not sure if needed: string hostname = 2;
}
