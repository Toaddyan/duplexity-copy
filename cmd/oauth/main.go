package main

func main() {
	// cfg := oauth.NewClientConfig("https://accounts.google.com")

	// http.HandleFunc("/auth/google/login", cfg.LoginUser)
	// http.HandleFunc("/auth/google/callback", cfg.CallbackHandler)

	// // Given an auth token, be able to authenicate any token.
	// //  automatic refresh for oauth, present to oauth provider along with refresh token.
	// //  - new refresh token and access token

	// // make it so that client will prompt the user to sit and wait
	// //  there until the auth service sends the id to the client

	// // be able to send this identity token to client

	// log.Printf("listening on http://%s/", "127.0.0.1:5556")
	// log.Fatal(http.ListenAndServe("127.0.0.1:5556", nil))

}
